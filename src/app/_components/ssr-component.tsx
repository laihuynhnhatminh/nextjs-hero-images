export default function SSRComponent() {
  return (
    <div>
      <h1>Server Side Rendered Component</h1>
      <p>
        This component is rendered on the server side. It is not re-rendered on
        the client side.
      </p>
      <p>
        This component is rendered on the server side. It is not re-rendered on
        the client side.
      </p>
      <p>
        This component is rendered on the server side. It is not re-rendered on
        the client side.
      </p>
      <p>
        This component is rendered on the server side. It is not re-rendered on
        the client side.
      </p>
      <p>
        This component is rendered on the server side. It is not re-rendered on
        the client side.
      </p>
      <p>
        This component is rendered on the server side. It is not re-rendered on
        the client side.
      </p>
      <p>
        This component is rendered on the server side. It is not re-rendered on
        the client side.
      </p>
      <p>
        This component is rendered on the server side. It is not re-rendered on
        the client side.
      </p>
      <p>
        This component is rendered on the server side. It is not re-rendered on
        the client side.
      </p>
      <p>
        This component is rendered on the server side. It is not re-rendered on
        the client side.
      </p>
      <p>
        This component is rendered on the server side. It is not re-rendered on
        the client side.
      </p>
      <p>
        This component is rendered on the server side. It is not re-rendered on
        the client side.
      </p>
      <p>
        This component is rendered on the server side. It is not re-rendered on
        the client side.
      </p>
      <p>
        This component is rendered on the server side. It is not re-rendered on
        the client side.
      </p>
      <p>
        This component is rendered on the server side. It is not re-rendered on
        the client side.
      </p>
      <p>
        This component is rendered on the server side. It is not re-rendered on
        the client side.
      </p>
      <p>
        This component is rendered on the server side. It is not re-rendered on
        the client side.
      </p>
      <p>
        This component is rendered on the server side. It is not re-rendered on
        the client side.
      </p>
    </div>
  );
}
